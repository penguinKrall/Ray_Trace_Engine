#version 450 		// Use GLSL 4.5
#define GLM_SWIZZLE

//layout(location = 0) in vec4 pos;
//layout(location = 1) in vec4 col;
//layout(location = 2) in vec4 tex;
//layout(location = 3) in vec4 nor;
//
//layout(push_constant) uniform PushModel {
//		mat4 model;
//} pushModel;
//
//
//layout(location = 0) out vec4 fragPos;	//vertex position (x, y, z)
//layout(location = 1) out vec2 texCoords;	//tex u v

void main() {
	
//fragPos = pushModel.model * pos;   
//texCoords = vec2(tex);
//gl_Position = vec4(fragPos) * vec4(1.0f, -1.0f, -1.0f, 1.0f);

}