#version 450

//layout(location = 0) in vec4 fragPos;
//layout(location = 1) in vec2 texCoords;
//
//layout(set = 0, binding = 2 ) uniform sampler2DMS diffuseTexture;
//
//layout(location = 0) out vec4 outColor;

void main() {

 //vec4 accumulatedColor = vec4(0.0);
 //
 //  for (int i = 0; i < 8; ++i) {
 //      accumulatedColor += texelFetch(diffuseTexture, ivec2((texCoords) * textureSize(diffuseTexture)), i);
 //  }
 //
 //  outColor = accumulatedColor / 8.0f;

}